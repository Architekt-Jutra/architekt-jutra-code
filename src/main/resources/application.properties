spring.application.name=aj

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/aj
spring.datasource.username=aj
spring.datasource.password=aj

# Disable Spring Boot Docker Compose integration (using Podman directly)
spring.docker.compose.enabled=false

# JWT Configuration
app.jwt.secret=${APP_JWT_SECRET:i/WZnrbvFqiPfShuZjGmc5kC7IXxRZfpueJEdgCzGFc=}
app.jwt.expiration-ms=86400000

# CORS Configuration
app.cors.allowed-origins=http://localhost:*

# Liquibase
spring.liquibase.contexts=dev
